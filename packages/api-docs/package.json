{
  "name": "api-docs",
  "description": "API Documentation",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "start": "(cd website && yarn start)",
    "build": "(cd website && yarn build)"
  }
}
